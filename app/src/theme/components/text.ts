import { ComponentStyleConfig } from "@chakra-ui/react"

export const textTheme: ComponentStyleConfig = {
  variants: {
    highlight: {
      textAlign: "center",
      background: "#f3f30d",
    },
  },
}
